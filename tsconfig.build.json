{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmitOnError": true, // Don't build if there are type errors
    "sourceMap": false // No source maps needed in published package
  },
  "exclude": ["node_modules", "dist", "test", "jest.config.js"]
}
